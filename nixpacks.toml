[phases.setup]
nixPkgs = ['python312']

[phases.install]
cmds = ['pip install -r requirements.txt']

[start]
cmd = 'streamlit run interactive_dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true'